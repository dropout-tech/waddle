/* eslint-disable no-console -- this executable regression script reports each check */
import { spawn } from 'node:child_process'
import { setTimeout as sleep } from 'node:timers/promises'
import { readFileSync, mkdirSync } from 'node:fs'
import { chromium } from 'playwright'
import assert from 'node:assert/strict'
const env = Object.fromEntries(readFileSync('.env.e2e.local','utf8').split('\n').filter(l=>l.includes('=')&&!l.startsWith('#')).map(l=>{const i=l.indexOf('=');return [l.slice(0,i),l.slice(i+1).trim().replace(/^['"]|['"]$/g,'')]}))
const base=process.env.E2E_BASE_URL||'http://localhost:3172'
const server=process.env.E2E_BASE_URL?null:spawn('pnpm',['exec','next','dev','-p','3172'],{stdio:'ignore',detached:true})
const today=new Date().toLocaleDateString('en-CA')
let rows=[{id:'00000000-0000-4000-8000-000000000001',date:today,type:'text',content:'原本上方的筆記',sort_order:0,metadata:{preserved:'yes'},created_at:new Date().toISOString()}]
let writes=[],fail=false,browser,passes=0
let notebookRow={id:'00000000-0000-4000-8000-000000000101',title:'記事本回歸測試',content:{type:'doc',content:[{type:'paragraph',content:[{type:'text',text:'原本記事本內容'}]}]},sort_order:0,created_at:new Date().toISOString(),updated_at:new Date().toISOString(),is_archived:false}
const check=(name,ok)=>{assert.ok(ok,name);console.log('PASS',name);passes++}
const settle = () => sleep(350)
const canvasEditor = page => page.getByTestId('scratchpad-canvas').getByLabel('畫布內容', { exact: true })
const blankPoint = async page => {
 await page.getByTestId('scratchpad-canvas').scrollIntoViewIfNeeded()
 return page.getByTestId('scratchpad-canvas').evaluate(el => {
  const b = el.getBoundingClientRect()
  for (let y = b.bottom - 35; y > b.top + 35; y -= 50) {
   for (let x = b.right - 35; x > b.left + 35; x -= 50) {
    if (document.elementFromPoint(x, y) === el) return { x, y }
   }
  }
  throw new Error('No visible blank canvas point')
 })
}
const blurEditor = async page => { await page.getByRole('heading', {name:'白板',exact:true}).click(); await settle() }

try {
 for(let i=0;i<120;i++){try{if((await fetch(base+'/login')).ok)break}catch{}await sleep(1000)}
 browser=await chromium.launch();const context=await browser.newContext({locale:'zh-TW',viewport:{width:1280,height:1000}});const page=await context.newPage()
 await context.route('**/rest/v1/**',async route=>{
  const r=route.request(),u=new URL(r.url()),table=u.pathname.split('/').pop(),id=u.searchParams.get('id')?.replace('eq.','')
  if(table==='notebook_notes'){if(r.method()==='GET')return route.fulfill({json:[notebookRow]});notebookRow={...notebookRow,...r.postDataJSON()};return route.fulfill({json:[]})}
  if(table!=='scratchpad_items')return route.fulfill({json:[]})
  if(r.method()==='GET')return route.fulfill({json:rows})
  const body=r.postData()?r.postDataJSON():null;writes.push({method:r.method(),body,id})
  if(fail)return route.fulfill({status:500,json:{message:'Synthetic failure'}})
  if(r.method()==='POST')rows.push({...body,created_at:new Date().toISOString()})
  if(r.method()==='PATCH')rows=rows.map(x=>x.id===id?{...x,...body}:x)
  if(r.method()==='DELETE')rows=rows.filter(x=>x.id!==id)
  return route.fulfill({json:r.method()==='PATCH'?{id}:[]})
 })
 await page.goto(base+'/login');await page.locator('#email').fill(env.E2E_EMAIL);await page.locator('#password').fill(env.E2E_PASSWORD);await page.locator('button[type=submit]').click();await page.waitForURL(u=>!u.pathname.includes('/login'),{timeout:60000})
 await page.goto(base+'/float/scratchpad');await page.getByTestId('scratchpad-canvas').waitFor();await page.getByTestId('canvas-item').getByText('原本上方的筆記',{exact:true}).waitFor()
 check('Legacy content is visible directly on the whiteboard',await page.getByTestId('canvas-item').getByText('原本上方的筆記',{exact:true}).isVisible())
 check('Loading legacy content preserves its ID and metadata without migration writes',rows.length===1&&rows[0].id==='00000000-0000-4000-8000-000000000001'&&rows[0].metadata.preserved==='yes'&&!rows[0].metadata.canvas&&writes.length===0)
 check('Whiteboard replaces upper quick cards and transfer controls',await page.getByRole('heading',{name:'白板',exact:true}).count()===1&&await page.getByLabel('移入上方卡片').count()===0&&await page.getByRole('button',{name:'移回上方',exact:true}).count()===0&&await page.locator('input[placeholder*="記下"]').count()===0)
 const card=page.getByTestId('canvas-item').first(),handle=card.getByTestId('canvas-drag-handle');await handle.scrollIntoViewIfNeeded()
 const originalGeometry=await card.evaluate(el=>({x:parseFloat(el.style.left),y:parseFloat(el.style.top),width:parseFloat(el.style.width),height:parseFloat(el.style.height)}))
 let b=await handle.boundingBox(),before=writes.length;await page.mouse.move(b.x+b.width/2,b.y+b.height/2);await page.mouse.down();await page.mouse.move(b.x+b.width/2+120,b.y+b.height/2+35,{steps:8});check('Drag preview makes no writes',writes.length===before);await page.mouse.up();await sleep(400)
 check('Drag writes once on release',writes.length===before+1&&rows[0].metadata.canvas.x===originalGeometry.x+120)
 const resize=card.getByTestId('canvas-resize-handle');b=await resize.boundingBox();await page.mouse.move(b.x+20,b.y+20);await page.mouse.down();await page.mouse.move(b.x+90,b.y+70,{steps:6});await page.mouse.up();await sleep(300)
 check('Resize geometry persisted',rows[0].metadata.canvas.width===originalGeometry.width+70&&rows[0].metadata.canvas.height===originalGeometry.height+50)
 await page.reload();await page.getByTestId('canvas-item').waitFor();check('Reload preserves placement',await card.evaluate((e,g)=>parseFloat(e.style.left)===g.x+120&&parseFloat(e.style.width)===g.width+70,originalGeometry))
 before=writes.length;for(let i=0;i<8;i++)await page.getByRole('button',{name:'縮小畫布',exact:true}).click();b=await handle.boundingBox();check('Move handle stays touch-sized when zoomed out',b.width>=40&&b.height>=40);await page.getByRole('button',{name:'顯示全部',exact:true}).click();check('Viewport changes do not write records',writes.length===before)
 await handle.focus();await page.keyboard.press('ArrowRight');await page.keyboard.press('ArrowRight');await sleep(350);check('Rapid keyboard movement keeps every step',rows[0].metadata.canvas.x===originalGeometry.x+160)
 const previous=rows[0].metadata.canvas.x;fail=true;await handle.focus();await page.keyboard.press('ArrowRight');await sleep(500);fail=false;check('Failed move rolls back',await card.evaluate((e,x)=>e.style.left===x+'px',previous))
 await handle.focus();await page.keyboard.press('ArrowRight');await sleep(350);check('Movement after rollback starts from persisted geometry',rows[0].metadata.canvas.x===previous+20)
 // Inline creation must remain a draft until leaving the object.
 before=writes.length;const countBeforeText=rows.length
 await page.getByRole('button',{name:'文字',exact:true}).click()
 let editor=canvasEditor(page);await editor.waitFor()
 check('Toolbar opens an autofocus editor inside the canvas',await editor.evaluate(e=>e===document.activeElement))
 check('Empty inline draft creates no record',writes.length===before&&rows.length===countBeforeText)
 await blurEditor(page)
 check('Leaving an empty draft cancels without writes',writes.length===before&&await editor.count()===0)
 await page.getByRole('button',{name:'文字',exact:true}).click();await editor.fill('取消的草稿');await editor.press('Escape');await settle()
 check('Escape cancels a new draft without writes',writes.length===before&&rows.length===countBeforeText&&await editor.count()===0)
 await page.getByRole('button',{name:'文字',exact:true}).click();await editor.fill('在畫布直接寫字')
 await editor.dispatchEvent('compositionstart')
 await editor.dispatchEvent('keydown',{key:'Enter',code:'Enter',isComposing:true,bubbles:true})
 await editor.dispatchEvent('keydown',{key:'Escape',code:'Escape',isComposing:true,bubbles:true})
 check('IME confirmation and Escape keep the inline editor open',await editor.count()===1&&writes.length===before)
 const compositionBlank=await blankPoint(page)
 await page.mouse.click(compositionBlank.x,compositionBlank.y)
 check('Canvas click does not save unfinished IME composition',await editor.count()===1&&writes.length===before)
 await editor.focus()
 await editor.dispatchEvent('compositionend',{data:'字'})
 await editor.press('Enter');await editor.press('End');await editor.type('第二行')
 check('Enter inserts a newline without prematurely saving',(await editor.inputValue()).includes('\n')&&writes.length===before)
 await blurEditor(page)
 const newText=rows.find(row=>row.content.includes('在畫布直接寫字'))
 check('Leaving a new text object creates exactly one record',!!newText&&rows.length===countBeforeText+1&&writes.length===before+1&&writes.at(-1).method==='POST')
 await page.getByRole('button',{name:'顯示全部',exact:true}).click()
 const textCard=page.locator(`[data-canvas-item="${newText.id}"]`)
 await textCard.getByText(newText.content,{exact:true}).dblclick();await editor.waitFor()
 const geometryBeforeEdit=JSON.stringify(newText.metadata.canvas),textId=newText.id
 before=writes.length
 const viewBeforeSelection=await page.getByTestId('scratchpad-canvas').evaluate(e=>e.firstElementChild.style.transform)
 b=await editor.boundingBox();await page.mouse.move(b.x+15,b.y+15);await page.mouse.down();await page.mouse.move(b.x+100,b.y+15,{steps:6});await page.mouse.up()
 check('Selecting text does not pan the canvas',viewBeforeSelection===await page.getByTestId('scratchpad-canvas').evaluate(e=>e.firstElementChild.style.transform)&&writes.length===before)
 await editor.fill('原地修改，保留物件');await blurEditor(page)
 check('Editing preserves the original ID and geometry',rows.filter(row=>row.id===textId).length===1&&rows.find(row=>row.id===textId).content==='原地修改，保留物件'&&JSON.stringify(rows.find(row=>row.id===textId).metadata.canvas)===geometryBeforeEdit&&writes.length===before+1&&writes.at(-1).method==='PATCH')
 await textCard.getByText('原地修改，保留物件',{exact:true}).dblclick();await editor.fill('不應儲存');before=writes.length;await editor.press('Escape');await settle()
 check('Escape discards edits to an existing object',rows.find(row=>row.id===textId).content==='原地修改，保留物件'&&writes.length===before)
 await page.reload();await textCard.waitFor()
 check('Reload preserves inline text content',await textCard.getByText('原地修改，保留物件',{exact:true}).isVisible())
 await textCard.getByText('原地修改，保留物件',{exact:true}).dblclick();await editor.fill('儲存失敗的文字');fail=true;await blurEditor(page);fail=false
 check('Failed text update restores persisted content and ID',rows.find(row=>row.id===textId).content==='原地修改，保留物件'&&await textCard.getByText('原地修改，保留物件',{exact:true}).isVisible())
 await page.getByRole('button',{name:'連結',exact:true}).click()
 const urlEditor=page.getByTestId('scratchpad-canvas').getByLabel('連結網址',{exact:true})
 before=writes.length;await urlEditor.fill('http://[');await blurEditor(page)
 check('Invalid inline link stays editable without a write',await urlEditor.count()===1&&writes.length===before)
 await urlEditor.fill('https://example.com/notes');await page.getByTestId('scratchpad-canvas').getByLabel('連結標題',{exact:true}).fill('研究參考');await blurEditor(page)
 const link=rows.find(row=>row.type==='link')
 check('Link URL and title save inline',link?.content==='https://example.com/notes'&&link.title==='研究參考')
 const linkCard=page.locator(`[data-canvas-item="${link.id}"]`)
 await linkCard.click({position:{x:100,y:10}});await linkCard.getByRole('button',{name:'編輯連結',exact:true}).click()
 await page.getByTestId('scratchpad-canvas').getByLabel('連結標題',{exact:true}).fill('');await blurEditor(page)
 check('Clearing link title persists an empty title on the same ID',rows.find(row=>row.id===link.id).title==='')
 await linkCard.getByTestId('canvas-resize-handle').focus()
 for(let i=0;i<3;i++)await page.keyboard.press('ArrowUp')
 await settle();check('Link can retain compact 160px geometry',rows.find(row=>row.id===link.id).metadata.canvas.height===160)
 await linkCard.getByRole('button',{name:'編輯連結',exact:true}).click();await urlEditor.waitFor()
 check('Minimum-height link editor contains URL title and footer',await linkCard.evaluate(el=>{const box=el.getBoundingClientRect();return [...el.querySelectorAll('form textarea, form input, form button')].every(control=>{const r=control.getBoundingClientRect();return r.top>=box.top&&r.bottom<=box.bottom+1&&r.left>=box.left&&r.right<=box.right+1})}))
 mkdirSync('/tmp/huddle-whiteboard-shots',{recursive:true});await page.screenshot({path:'/tmp/huddle-whiteboard-shots/link-minheight-editing.png',fullPage:true})
 await page.getByTestId('scratchpad-canvas').getByLabel('連結標題',{exact:true}).fill('保留緊湊尺寸');await blurEditor(page)
 check('Saving compact link edits preserves its stored height',rows.find(row=>row.id===link.id).metadata.canvas.height===160&&rows.find(row=>row.id===link.id).title==='保留緊湊尺寸')
 // Exercise placement after both zoom and pan: screen coordinates must convert to world coordinates.
 await page.getByRole('button',{name:'縮小畫布',exact:true}).click()
 let point=await blankPoint(page);await page.mouse.move(point.x,point.y);await page.mouse.down();await page.mouse.move(point.x-65,point.y-30,{steps:5});await page.mouse.up()
 point=await blankPoint(page)
 const expected=await page.getByTestId('scratchpad-canvas').evaluate((el,p)=>{const r=el.getBoundingClientRect(),m=new DOMMatrix(getComputedStyle(el.firstElementChild).transform);return {x:(p.x-r.left-m.e)/m.a,y:(p.y-r.top-m.f)/m.d}},point)
 before=writes.length;await page.mouse.dblclick(point.x,point.y);await editor.waitFor();await editor.fill('雙擊定位文字');await blurEditor(page)
 const placed=rows.find(row=>row.content==='雙擊定位文字')
 check('Double click places text at the clicked point after pan and zoom',!!placed&&Math.abs(placed.metadata.canvas.x-expected.x)<3&&Math.abs(placed.metadata.canvas.y-expected.y)<3&&writes.length===before+1)
 // A whiteboard object opens a full notebook editor, preserving its identity and geometry.
 await page.setViewportSize({width:1280,height:1000});await page.getByRole('button',{name:'顯示全部',exact:true}).click()
 before=writes.length;await textCard.getByTestId('canvas-drag-handle').focus();await textCard.getByRole('button',{name:'開啟內容',exact:true}).click()
 const detail=page.getByRole('dialog',{name:'白板內容',exact:true}),rich=detail.locator('.tiptap')
 await rich.waitFor();await settle()
 check('Opening detail shows notebook editor without writing',await rich.getAttribute('contenteditable')==='true'&&writes.length===before)
 await detail.getByRole('button',{name:'返回白板',exact:true}).click();await settle();check('Closing unchanged detail does not write',writes.length===before);await textCard.getByTestId('canvas-drag-handle').focus();await textCard.getByRole('button',{name:'開啟內容',exact:true}).click();await rich.waitFor()
 await detail.getByLabel('標題',{exact:true}).fill('深入整理');await rich.fill('');await rich.pressSequentially('/h1');await page.getByRole('listbox',{name:'插入區塊'}).waitFor();await page.getByRole('option').filter({hasText:'標題 1'}).click();await rich.pressSequentially('白板內頁標題');await rich.press('Enter');await rich.pressSequentially('/todo');await page.getByRole('listbox',{name:'插入區塊'}).waitFor();await rich.press('Enter');await rich.pressSequentially('第一個檢查項目');await rich.press('Enter');await rich.pressSequentially('第二個檢查項目')
 await rich.locator('input[type=checkbox]').first().check();await sleep(700)
 const documentRow=()=>rows.find(row=>row.id===textId)
 check('Notebook heading and checklist persist as structured document',documentRow().metadata.document.content.some(n=>n.type==='heading')&&JSON.stringify(documentRow().metadata.document).includes('taskList')&&JSON.stringify(documentRow().metadata.document).includes('"checked":true'))
 check('Detail edits preserve original ID geometry and title',rows.filter(row=>row.id===textId).length===1&&JSON.stringify(documentRow().metadata.canvas)===geometryBeforeEdit&&documentRow().title==='深入整理')
 mkdirSync('/tmp/huddle-whiteboard-shots',{recursive:true});await page.screenshot({path:'/tmp/huddle-whiteboard-shots/desktop-detail.png',fullPage:true})
 await detail.getByRole('button',{name:'返回白板',exact:true}).click();await detail.waitFor({state:'hidden'})
 await page.reload();await textCard.waitFor();await textCard.getByTestId('canvas-drag-handle').focus();await textCard.getByRole('button',{name:'開啟內容',exact:true}).click();await rich.waitFor()
 check('Reload and reopening retain notebook heading and checkbox state',await rich.locator('h1').innerText()==='白板內頁標題'&&await rich.locator('input[type=checkbox]').count()===2&&await rich.locator('input[type=checkbox]').first().isChecked())
 await page.setViewportSize({width:390,height:844});await page.screenshot({path:'/tmp/huddle-whiteboard-shots/mobile-detail.png',fullPage:true})
 check('Mobile detail fits viewport and exposes notebook formatting',await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)&&await detail.getByRole('button',{name:'待辦清單',exact:true}).count()===1)
 const mobileTaskLabel=rich.locator('label').filter({has:page.locator('input[type=checkbox]')}).nth(1);const mobileTaskBounds=await mobileTaskLabel.boundingBox();check('Mobile checklist labels provide 44px touch targets',mobileTaskBounds.width>=44&&mobileTaskBounds.height>=44);await mobileTaskLabel.click({position:{x:mobileTaskBounds.width-3,y:mobileTaskBounds.height-3}});await sleep(700)
 check('Mobile checklist toggles persist independently',documentRow().metadata.document.content.find(n=>n.type==='taskList').content.every(n=>n.attrs.checked===true))
 await detail.getByRole('button',{name:'返回白板',exact:true}).click();await page.setViewportSize({width:1280,height:1000})
 // Native link edits must stay synchronized with its richer document without losing annotations.
 const linkGeometry=JSON.stringify(rows.find(row=>row.id===link.id).metadata.canvas)
 await page.getByRole('button',{name:'顯示全部',exact:true}).click();await linkCard.getByTestId('canvas-drag-handle').focus();await linkCard.getByRole('button',{name:'開啟內容',exact:true}).click();await rich.waitFor()
 check('Link detail initially contains the original source URL',await rich.locator('a').first().getAttribute('href')==='https://example.com/notes')
 await rich.click();await rich.press('ControlOrMeta+End');await rich.press('Enter');await rich.pressSequentially('補充說明必須保留');await detail.getByRole('button',{name:'返回白板',exact:true}).click();await settle()
 await linkCard.getByTestId('canvas-drag-handle').focus();await linkCard.getByRole('button',{name:'編輯連結',exact:true}).click();await urlEditor.fill('https://example.com/updated');await blurEditor(page)
 await linkCard.getByTestId('canvas-drag-handle').focus();await linkCard.getByRole('button',{name:'開啟內容',exact:true}).click();await rich.waitFor()
 check('Updating the native link URL also updates the reopened document source',await rich.locator('a').first().getAttribute('href')==='https://example.com/updated'&&(await rich.innerText()).includes('https://example.com/updated'))
 check('Link URL synchronization preserves annotations ID and geometry',(await rich.innerText()).includes('補充說明必須保留')&&rows.filter(row=>row.id===link.id).length===1&&JSON.stringify(rows.find(row=>row.id===link.id).metadata.canvas)===linkGeometry)
 await detail.getByRole('button',{name:'返回白板',exact:true}).click()
 before=writes.length;await page.getByRole('button',{name:'檢查清單',exact:true}).click();await rich.waitFor()
 check('Checklist tool opens a structured editable checklist',await rich.locator('input[type=checkbox]').count()===1)
 await rich.locator('p').first().click();await page.keyboard.type('新檢查清單項目');await detail.getByRole('button',{name:'返回白板',exact:true}).click();await sleep(700)
 check('New checklist saves its document on the whiteboard',rows.some(row=>row.content.includes('新檢查清單項目')&&JSON.stringify(row.metadata.document).includes('taskList'))&&writes.length>before)
 const beforeDrop=rows.length;await page.evaluate(() => { const transfer=new DataTransfer();transfer.items.add(new File([Uint8Array.from(atob('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAwMCAO+j5ZkAAAAASUVORK5CYII='),c=>c.charCodeAt(0))],'drop.png',{type:'image/png'}));const canvas=document.querySelector('[data-testid="scratchpad-canvas"]');canvas.dispatchEvent(new DragEvent('drop',{bubbles:true,cancelable:true,dataTransfer:transfer})); });await sleep(300)
 check('Dropping over whiteboard creates one canvas image',rows.length===beforeDrop+1&&rows.at(-1).type==='image'&&rows.at(-1).metadata?.canvas)
 await page.getByRole('button',{name:'畫筆',exact:true}).click();const canvas=page.getByTestId('scratchpad-canvas');b=await canvas.boundingBox();await page.mouse.move(b.x+b.width-100,b.y+b.height-100);await page.mouse.down();await page.mouse.move(b.x+b.width-40,b.y+b.height-40,{steps:8});await page.mouse.up();await sleep(300)
 check('Pen stroke persists as a resizable canvas image',rows.some(row=>row.type==='image'&&row.title==='手寫筆記'&&row.metadata?.canvas))
 await page.getByRole('button',{name:'畫筆',exact:true}).click()
 mkdirSync('/tmp/huddle-whiteboard-shots',{recursive:true})
 await page.setViewportSize({width:1280,height:1000});await page.getByRole('button',{name:'文字',exact:true}).click();await editor.fill('專注當下\n把想到的事，直接寫在這裡。');await page.screenshot({path:'/tmp/huddle-whiteboard-shots/desktop-editing.png',fullPage:true});await editor.press('Escape')
 await page.setViewportSize({width:390,height:844})
 await page.getByRole('button',{name:'文字',exact:true}).click();await editor.waitFor()
 check('Mobile inline editor remains inside visible canvas',await editor.evaluate(e=>{const r=e.getBoundingClientRect(),c=e.closest('[data-testid="scratchpad-canvas"]').getBoundingClientRect();return r.left>=c.left&&r.right<=c.right+1&&parseFloat(getComputedStyle(e).fontSize)>=16}))
 await editor.fill('手機直接編輯');await page.screenshot({path:'/tmp/huddle-whiteboard-shots/mobile-editing.png',fullPage:true});await blurEditor(page)
 check('Mobile direct editing saves its text',rows.some(row=>row.content==='手機直接編輯'))
 mkdirSync('/tmp/huddle-whiteboard-shots',{recursive:true})
 for(const width of [320,390,430,1280]){await page.setViewportSize({width,height:844});await page.getByTestId('scratchpad-canvas').scrollIntoViewIfNeeded();await sleep(100);check(`No document overflow at ${width}`,await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await page.screenshot({path:`/tmp/huddle-whiteboard-shots/${width}.png`,fullPage:true})}
 await page.setViewportSize({width:1280,height:1000});await page.getByRole('button',{name:'顯示全部',exact:true}).click()
 const yesterday=new Date();yesterday.setDate(yesterday.getDate()-1)
 rows.push({id:'00000000-0000-4000-8000-000000000099',date:yesterday.toLocaleDateString('en-CA'),type:'text',content:'昨天的唯讀筆記',sort_order:0,metadata:{document:{type:'doc',content:[{type:'paragraph',content:[{type:'text',text:'昨天的唯讀筆記'}]}]},canvas:{x:0,y:0,width:280,height:220}},created_at:yesterday.toISOString()})
 await page.reload();await page.getByTestId('scratchpad-canvas').waitFor()
 await page.locator('button').filter({has:page.locator('svg.lucide-chevron-left')}).click()
 await page.getByText('昨天的唯讀筆記',{exact:true}).waitFor();before=writes.length
 await page.getByText('昨天的唯讀筆記',{exact:true}).dblclick()
 await rich.waitFor();check('Historical detail opens read-only',await rich.getAttribute('contenteditable')==='false'&&await detail.getByLabel('標題',{exact:true}).getAttribute('readonly')!==null&&writes.length===before);await detail.getByRole('button',{name:'返回白板',exact:true}).click()
 point=await blankPoint(page);await page.mouse.dblclick(point.x,point.y);await settle()
 check('Historical canvas remains read-only for existing and blank double clicks',await editor.count()===0&&writes.length===before&&await page.getByRole('button',{name:'文字',exact:true}).count()===0)
 await page.setViewportSize({width:1280,height:1000});await page.goto(base+'/notebook');await page.getByText('記事本回歸測試',{exact:true}).click();const notebookEditor=page.locator('.tiptap');await notebookEditor.waitFor()
 check('Original notebook still loads an editable document',await notebookEditor.getAttribute('contenteditable')==='true'&&(await notebookEditor.innerText()).includes('原本記事本內容'))
 await page.getByLabel('標題',{exact:true}).fill('記事本修改仍正常');await notebookEditor.fill('記事本原有編輯正常');await sleep(1200)
 check('Original notebook title and content still save',notebookRow.title==='記事本修改仍正常'&&JSON.stringify(notebookRow.content).includes('記事本原有編輯正常'))
 console.log(`${passes} checks passed; ${writes.length} mocked whiteboard writes; notebook writes also mocked`)


} finally {await browser?.close();if(server)try{process.kill(-server.pid,'SIGTERM')}catch{}}
